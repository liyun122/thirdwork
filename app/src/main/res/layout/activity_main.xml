<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_layout"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/add_et_todo"
        android:enabled="false"
        android:hint="事项"
        android:gravity="top"
        android:visibility="invisible"
        android:background="#7ACAEE"
        android:textSize="20sp"
        android:textColor="#000"
        android:layout_width="0dp"
        android:layout_height="80dp"
        android:layout_marginTop="20dp"
        android:layout_marginLeft="10dp"
        android:padding="5dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintWidth_percent="0.6"
        />
    <EditText
        android:id="@+id/add_et_date"
        android:hint="时间"
        android:gravity="top"
        android:visibility="invisible"
        android:background="#7ACAEE"
        android:textSize="20sp"
        android:textColor="#000"
        android:layout_width="0dp"
        android:layout_height="35dp"
        android:padding="5dp"
        android:layout_marginTop="5dp"
        android:cursorVisible="false"
        android:focusable="false"
        android:focusableInTouchMode="false"
        app:layout_constraintTop_toBottomOf="@+id/add_et_todo"
        app:layout_constraintLeft_toLeftOf="@+id/add_et_todo"
        app:layout_constraintWidth_percent="0.6"
        />
    <Button
        android:id="@+id/add_bt_add"
        android:text="添加"
        android:textSize="30dp"
        android:gravity="center"
        android:layout_width="0dp"
        android:layout_height="70dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="@+id/add_et_date"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_margin="10dp" />
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/todo_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@+id/add_et_date"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_margin="8dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>